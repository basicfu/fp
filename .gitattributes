# 仓库内一律使用 LF。
#
# 没有这个文件时，Windows 上 core.autocrlf=true 会在每次 checkout 把工作区
# 重写成 CRLF，于是 gofmt 把所有 .go 文件都报成未格式化——本仓库合并到
# master 时就是这么发作的。scripts/*.sh 更严重：CRLF 会让 shebang 失效，
# 靠 ./scripts/test.sh 直接执行会失败。
* text=auto eol=lf

# 二进制不做行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
